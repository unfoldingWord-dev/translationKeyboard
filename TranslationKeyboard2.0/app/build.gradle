apply plugin: 'com.android.application'

android {
    signingConfigs {
        release {
            keyAlias 'KEY_ALIAS'
            keyPassword 'KEY_PASSWORD'
            storeFile file('PATH_TO_FILE')
            storePassword 'KEYSTORE_PASSWORD'
        }
    }
    compileSdkVersion 21
    buildToolsVersion "21.1.1"
    defaultConfig {
        applicationId 'org.distantshoresmedia.translationkeyboard'
        minSdkVersion 8
        targetSdkVersion 21
        versionCode 13
        versionName '2.0.0'
        testApplicationId 'org.distantshoresmedia.translationkeyboardtest'
        signingConfig signingConfigs.release
    }
    buildTypes {
        release {
            minifyEnabled true
            signingConfig signingConfigs.release
        }
    }
    lintOptions {
        abortOnError false
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:21.0.3'
    compile 'com.joanzapata.android:android-iconify-parent:1.0.9'
    compile 'com.joanzapata.android:android-iconify:1.0.9'
}
